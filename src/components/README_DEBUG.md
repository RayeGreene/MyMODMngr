Turn on card debug overlay

Set this flag in DevTools console to show per-card computed info (category, tags):

localStorage.setItem('mm-debug-cards', '1');

Turn off:

localStorage.removeItem('mm-debug-cards');
